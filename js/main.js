/*
" use strict";

(function () {
// Активация страницы

  const map = window.data.map;
  let getHotelArray = window.data.getHotelArray;
  const renderFragmentMapPins = window.map.renderFragmentMapPins;
  const adForm = window.form.adForm;
  const setCoordinates = window.form.setCoordinates;
  const mapPinMain = window.form.mapPinMain;
  const showElements = window.form.showElements;

  const mapPinMainClick = window.form.mapPinMainClick;


  window.main = {
    activatePage,
    mapPinMainClick
  };

})();
 */
